<p align="center"><img src="assets/cheburnet-scripts-banner.jpg" alt="ЧебурNET — авторские инструменты для Linux-серверов" width="100%"></p>
<h1 align="center">ЧебурNET · Traffic Control</h1>
<p align="center">Управление фильтрацией входящего трафика Linux-сервера</p>
<p align="center">
  <a href="https://github.com/leonidkopysov/CheburNET-Traffic-Control/releases/tag/v1.0.0"><img src="https://img.shields.io/badge/версия-1.0.0-00bcd4" alt="Версия 1.0.0"></a>
  <a href="https://github.com/leonidkopysov/CheburNET-Traffic-Control/actions/workflows/test.yml"><img src="https://github.com/leonidkopysov/CheburNET-Traffic-Control/actions/workflows/test.yml/badge.svg" alt="Автоматические проверки"></a>
  <a href="LICENSE"><img src="https://img.shields.io/badge/лицензия-MIT-00bcd4" alt="Лицензия MIT"></a>
  <img src="https://img.shields.io/badge/Python-3.10%2B-3776ab" alt="Python 3.10 и новее">
</p>
<p align="center">Автор и разработчик: <b>Леонид Копысов</b><br>
<a href="https://github.com/leonidkopysov">GitHub · leonidkopysov</a> ·
<a href="https://t.me/kopysovleonid">Telegram · @kopysovleonid</a></p>

Самостоятельный менеджер блокировок IPv4/IPv6 на Python и nftables. Загружает три внешних списка,
применяет правила, обновляет их по расписанию и показывает состояние на русском языке.
Управление — через меню или короткую команду `ctc`.

**[Установка](#установка) · [Команды](#команды) · [Обновление](docs/UPDATE.md) · [Диагностика](#диагностика-и-журналы) · [Поддержка](SUPPORT.md)**

## Возможности

| Компонент | Назначение |
|---|---|
| Фильтрация | Собственная таблица `inet cheburnet_tc`, входящий IPv4 и IPv6 |
| Внешние списки | `antiscanner`, `government_networks`, `skipa` |
| Ручное управление | Блокировки IP/CIDR и исключения отдельных IP |
| Обновления | Загрузка всех источников, проверка и применение правил |
| Автозапуск | Восстановление локальных правил после перезагрузки |
| Статистика | Топ IP по журналу блокировок и организации через RDAP |
| Самодиагностика | Зависимости, конфигурация, права, правила, службы и таймеры |
| Интерфейс | Русское меню, отчёт о компонентах, адаптивная ширина и фирменная шапка |

## Быстрая установка

Рабочая короткая команда с автоматическим определением IP текущего SSH-подключения:

```bash
curl -fsSL https://github.com/leonidkopysov/CheburNET-Traffic-Control/releases/download/v1.0.0/cheburnet-traffic-control.py -o ctc.py && sudo env SSH_CONNECTION="${SSH_CONNECTION:-}" python3 ctc.py install
```

Скрипт будет сохранён в текущем каталоге как `ctc.py`, после чего запустится интерактивная установка.
Для установки с обязательной проверкой контрольной суммы используйте полный вариант ниже.

## Установка с проверкой SHA-256

Нужны **root, Python 3.10+ и работающий systemd**. Целевые ОС — Ubuntu 22.04/24.04 и Debian 12/13.
Недостающие `nftables` и `ca-certificates` устанавливаются через apt после согласия на установку.
Python должен быть установлен заранее. Практическая проверка прежней сборки проводилась на Ubuntu 24.04.4;
полная матрица ОС не проверялась.

Скачайте скрипт и контрольную сумму из фиксированного релиза **v1.0.0**:

```bash
curl -fsSLo cheburnet-traffic-control.py \
  https://raw.githubusercontent.com/leonidkopysov/CheburNET-Traffic-Control/v1.0.0/cheburnet-traffic-control.py &&
curl -fsSLo SHA256SUMS \
  https://raw.githubusercontent.com/leonidkopysov/CheburNET-Traffic-Control/v1.0.0/SHA256SUMS &&
sha256sum -c SHA256SUMS &&
sudo env SSH_CONNECTION="${SSH_CONNECTION:-}" python3 ./cheburnet-traffic-control.py install
```

Скрипт покажет аннотацию и запросит подтверждение установки. IP администратора и порт SSH предлагаются
из текущего подключения: подтвердите их или введите свои. Для панели используются подходящие настройки
Vision Installer (`panel_ips` в `/opt/remnanode/settings.json`), иначе потребуется ручной ввод.
Нужен **исходящий IP панели**: адрес домена за CDN может отличаться от него.

После установки появятся отчёт о компонентах и главное меню. Выберите **«Включить фильтрацию»**
или выполните:

```bash
sudo ctc on
sudo ctc s
```

Включение сразу применяет правила и настраивает восстановление после перезагрузки и ежедневное обновление.
Дополнительного подтверждения включения нет. Проверьте исключения администратора и панели перед включением,
а после — новое SSH-подключение и связь панели с нодой. Держите доступной консоль провайдера.

Существующий TrafficGuard требуется сначала штатно удалить; автоматического переноса его настроек нет.
Если Traffic Control уже установлен, используйте **[инструкцию обновления](docs/UPDATE.md)**.

<details>
<summary>Установка с явно заданными параметрами</summary>

Замените адреса и порт примера своими значениями. `--yes` пропускает вопросы, `--allow` можно повторять.
Параметры из `SSH_CONNECTION` в этом режиме не добавляются.

```bash
sudo python3 ./cheburnet-traffic-control.py install \
  --yes --ssh-port 22 --allow 198.51.100.10 --allow 198.51.100.20
sudo ctc on
```

</details>

## Команды

Главное меню: **`sudo ctc`**. Для root префикс `sudo` не нужен.

| Команда | Назначение |
|---|---|
| `ctc s` | Состояние, дата обновления, количество записей и исключения |
| `ctc t` | Топ-10 IP и организаций за 24 часа |
| `ctc t --no-resolve` | Топ без обращений к RDAP |
| `ctc l` | Последние блокировки и журнал обновлений |
| `ctc c` | Самодиагностика |
| `ctc fix` | Исправление обнаруженных проблем с подтверждением |
| `ctc r` | Полные правила nftables |
| `ctc u` | Обновить три списка и применить их, если фильтрация включена |
| `ctc on` | Включить фильтрацию, восстановление и автообновление |
| `ctc off` | Выключить фильтрацию и автообновление |
| `ctc --help` | Справка по полным командам |
| `ctc --version` | Версия установленного файла |

Команды для списков (вместо примеров укажите нужные IP):

```bash
sudo ctc ban 203.0.113.0/24
sudo ctc unban 203.0.113.0/24
sudo ctc allow 203.0.113.7
sudo ctc disallow 203.0.113.7
```

`unban` удаляет точную ручную запись. Адрес может остаться в другом диапазоне или внешнем списке.
`allow` добавляет отдельный IP в исключения, которые имеют приоритет над блокировками.
Последнее исключение удалить нельзя. Машинный статус: `ctc status --json`.

## Как применяются правила

1. Loopback и установленные/связанные соединения пропускаются этой таблицей.
2. Настроенные TCP-порты SSH пропускаются этой таблицей.
3. IP администратора, панели и другие исключения пропускаются этой таблицей.
4. Новые обращения от IP из объединённых внешних и ручных списков блокируются.

**Настроенные SSH-порты исключены из блокировок Traffic Control — в том числе для адресов из списков.**
Попытки входа могут оставаться в журнале sshd. Их ограничивают настройки SSH, Fail2ban и другие правила
firewall. `return` в таблице ЧебурNET не отменяет ограничения UFW или других таблиц.

Охватывается **INPUT**: сам сервер и контейнеры с `network_mode: host`. Docker bridge, транзитный
и исходящий трафик не фильтруются. Это фильтр по спискам адресов; обнаружение любых сканов по поведению
и защита пропускной способности канала в него не входят. Внешние списки могут давать ложные блокировки.

## Автоматическое обновление

После включения работает `cheburnet-traffic-control-update.timer`: первый запуск запланирован
через 15 минут после загрузки, следующие — примерно раз в сутки, с задержкой до 30 минут.
Ошибка любого источника останавливает обновление: прежние списки сохраняются.
При выключенной фильтрации ручная `ctc u` обновляет локальные данные без включения правил.

```bash
systemctl list-timers cheburnet-traffic-control-update.timer --all --no-pager --full
systemctl show cheburnet-traffic-control-update.service -p Result -p ExecMainStatus
```

Таймер обновляет **списки и правила**. Новый программный релиз устанавливается отдельно
по [инструкции обновления](docs/UPDATE.md).

## Диагностика и журналы

```bash
sudo ctc c
sudo ctc s
sudo ctc t
```

`sudo ctc fix` предлагает восстановление файлов, служб и правил, сохраняя выбор включённой/выключенной
фильтрации. После сторонней очистки firewall эта команда может восстановить таблицу.

Пустой топ означает отсутствие подходящих записей журнала, а не доказательство неисправности.
Топ считает до 10 000 записей за 24 часа. Логирование ограничено по частоте; колонка «Пакетов»
в топе отражает журнал. Полные счётчики совпадений смотрите в правилах:

```bash
sudo nft list chain inet cheburnet_tc ingress
```

Ненулевой `counter packets` перед `drop` показывает блокировки с момента создания текущей таблицы.
Счётчики сбрасываются при её пересоздании или перезагрузке. Добавленная блокировка не разрывает
установленное соединение — проверять нужно новое подключение.

RDAP передаёт IP из топа внешнему сервису и реестрам, кэширует результат на семь дней и показывает
владельца сети, который не обязательно является отправителем обращения. Без внешних запросов:
`ctc t --no-resolve`. При пустом топе выводится одна строка. В низком терминале меню показывает до пяти
записей, отдельная команда `ctc t` — до десяти. Цвета отключаются при `NO_COLOR` и перенаправлении вывода;
псевдографика требует терминал UTF-8.

Логирование включено по умолчанию; `--no-logging` при установке отключает его и сбор топа.
Срок хранения записей задаётся системным journald.

## Удаление

```bash
sudo ctc off
sudo ctc uninstall --yes
```

Удаляются собственная таблица, программа, короткая команда и службы. Конфигурация остаётся
в `/var/lib/cheburnet-traffic-control/`; повторная установка поверх неё намеренно запрещена.

## Документация проекта

- [Первый релиз 1.0.0](docs/RELEASE-1.0.0.md)
- [Обновление установленной версии](docs/UPDATE.md)
- [Поддержка и сообщения об ошибках](SUPPORT.md)
- [Сообщения об уязвимостях](SECURITY.md)
- [Участие в разработке](CONTRIBUTING.md)
- [Сторонние компоненты и источники](THIRD_PARTY_NOTICES.md)

Проверки запускаются в GitHub Actions на Python 3.10 и 3.12:

```bash
python3 -m unittest discover -s tests -v
python3 -m py_compile cheburnet-traffic-control.py
sha256sum -c SHA256SUMS
```

Тесты проверяют логику и сценарии ошибок с подменой системных вызовов. Они не заменяют проверку
сетевого трафика, загрузки ОС и совместимости firewall на конкретном сервере.

## Автор и лицензия

**Леонид Копысов** — автор и разработчик ЧебурNET.
[GitHub: leonidkopysov](https://github.com/leonidkopysov) · [Telegram: @kopysovleonid](https://t.me/kopysovleonid).

Оригинальный код — [MIT](LICENSE). Внешние списки и системные компоненты сохраняют собственные лицензии
и авторство. Исполняемый код TrafficGuard не включается и не запускается.

Другие продукты ЧебурNET:
[Auto Tuning](https://github.com/leonidkopysov/CheburNET-Auto-Tuning) ·
[Vision Installer](https://github.com/leonidkopysov/CheburNET-Vision-Installer).
